@{
    ViewData["Title"] = "Shopping Cart";
}

<div class="text-center">
    <h1>Corey's Knick Knacks</h1>
    <h2>Shopping Cart</h2>
</div>

@*@model Product
<div class="card" style="width: 18rem; padding: 5px;">
    <div class="card-body">
        <h5 class="card-title text-center">@Model.Id</h5>
        <p class="card-text">@Model.Price.ToString("c")</p>
        <p class="card-text">Quantity Available: @Model.Quantity</p>
        <br />
    </div>
    <div class="card-footer">
        <input class="num-spinner" id="spinner-@Model.Id" style="padding: 10px; width: 75%;" type="number" value="0" min="0" max="@Model.Quantity" step="1" />
    </div>
</div>*@